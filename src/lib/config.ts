
// Set this to true to enable authentication headers in API calls.
export const USE_AUTH = process.env.NEXT_PUBLIC_USE_AUTH === 'true';

// Set this to true to use local dummy data instead of fetching from the API.
export const USE_DUMMY_TEMPLATES = process.env.NEXT_PUBLIC_USE_DUMMY_TEMPLATES === 'true';

    